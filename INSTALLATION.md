# 🛠️ Инструкция по локальному запуску

Эта инструкция поможет тебе поднять Nocto Finance Dashboard локально на твоём компьютере.

## Предварительные требования

Убедись, что установлено:

- **Node.js** v18+ (проверь: `node --version`)
- **npm** или **yarn**
- **PostgreSQL** 14+ (локально или Docker)
- **Git**

---

## Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Volkofff666/nocto-finance-dashboard.git
cd nocto-finance-dashboard
```

---

## Шаг 2: Настройка Backend

### 2.1. Установка зависимостей

```bash
cd server
npm install
```

### 2.2. Настройка PostgreSQL

#### Вариант A: Локальный PostgreSQL

1. Установи PostgreSQL: [https://www.postgresql.org/download/](https://www.postgresql.org/download/)
2. Создай базу данных:

```sql
CREATE DATABASE nocto_finance;
```

#### Вариант B: Docker (PostgreSQL в контейнере)

```bash
docker run --name nocto-postgres \
  -e POSTGRES_PASSWORD=nocto2025 \
  -e POSTGRES_DB=nocto_finance \
  -p 5432:5432 \
  -d postgres:14
```

### 2.3. Настройка переменных окружения

Скопируй пример конфига:

```bash
cp .env.example .env
```

Отредактируй `.env` (важно!):

```env
DATABASE_URL="postgresql://postgres:nocto2025@localhost:5432/nocto_finance"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
PORT=3000
```

**Пояснения:**
- `DATABASE_URL` — строка подключения к PostgreSQL  
  Формат: `postgresql://<user>:<password>@<host>:<port>/<database>`
- `JWT_SECRET` — секретный ключ для JWT-токенов (в production используй сложный ключ!)

### 2.4. Применение миграций Prisma

```bash
npx prisma migrate dev --name init
```

Это создаст таблицы в базе данных:
- `User` — пользователи
- `Transaction` — транзакции
- `Proposal` — коммерческие предложения

### 2.5. Создание первого пользователя

#### Вариант A: Через Prisma Studio (удобно!)

```bash
npx prisma studio
```

Откроется браузер на `http://localhost:5555`:

1. Перейди в таблицу **User**
2. Нажми **Add record**
3. Заполни:
   - `email`: `admin@nocto.ru`
   - `password`: `$2a$10$...` (хеш от `password`, см. ниже)
   - `name`: `Admin`
   - `role`: `admin`
4. **Save**

**Генерация хеша пароля:**

```bash
node -e "console.log(require('bcryptjs').hashSync('password', 10))"
```

Скопируй полученный хеш в поле `password`.

#### Вариант B: Через API (после запуска сервера)

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@nocto.ru",
    "password": "password",
    "name": "Admin",
    "role": "admin"
  }'
```

### 2.6. Запуск Backend

```bash
npm run dev
```

Сервер запустится на **`http://localhost:3000`**

👉 Оставь этот терминал открытым!

---

## Шаг 3: Настройка Frontend

Открой **новый терминал**.

### 3.1. Установка зависимостей

```bash
cd client
npm install
```

### 3.2. Настройка переменных окружения

Создай файл `.env` в папке `client`:

```bash
touch .env
```

Добавь строку:

```env
VITE_API_URL=http://localhost:3000/api
```

### 3.3. Запуск Frontend

```bash
npm run dev
```

Frontend откроется на **`http://localhost:5173`**

---

## Шаг 4: Проверка работы

### 4.1. Вход в систему

Открой браузер: `http://localhost:5173`

Введи данные:
- **Email:** `admin@nocto.ru`
- **Password:** `password`

### 4.2. Проверь функционал

#### Dashboard
- Проверь отображение KPI (доходы, расходы, прибыль)
- Графики Chart.js должны отрисовываться
- Таблица транзакций (пока пустая)

#### Фильтры по датам
- Прокрути до таблицы транзакций
- Выбери диапазон дат или нажми быстрые фильтры (7Д, 30Д, 90Д)
- Нажми **"Применить"**

#### Генератор КП
- Перейди на страницу **"Генератор КП"** (в sidebar)
- Заполни данные клиента
- Добавь проблемы и услуги (используй быстрые кнопки)
- Проверь, что сумма автоматически рассчитывается
- Нажми **"Сохранить черновик"** → КП сохранится в базе
- Нажми **"Экспортировать PDF"** → скачается PDF-файл

---

## Полезные команды

### Backend

```bash
# Просмотр базы данных
npx prisma studio

# Применение новых миграций
npx prisma migrate dev

# Очистка базы данных (удалит все данные!)
npx prisma migrate reset

# Запуск с отладкой
DEBUG=* npm run dev
```

### Frontend

```bash
# Продакшен сборка
npm run build
npm run preview

# Проверка lint
npm run lint
```

### Docker (опционально)

Если используешь PostgreSQL в Docker:

```bash
# Остановить контейнер
docker stop nocto-postgres

# Запустить снова
docker start nocto-postgres

# Посмотреть логи
docker logs nocto-postgres

# Удалить контейнер
docker rm nocto-postgres
```

---

## Распространённые проблемы

### Ошибка: "Database connection failed"

Проверь:
- PostgreSQL запущен (`docker ps` или `sudo systemctl status postgresql`)
- Правильно указан `DATABASE_URL` в `.env`
- Порт 5432 не занят другим процессом

### Ошибка: "JWT must be provided"

Проверь:
- `JWT_SECRET` установлен в `server/.env`
- Ты залогинен в системе

### Ошибка: "CORS error"

Убедись, что:
- Backend запущен на `http://localhost:3000`
- Frontend на `http://localhost:5173`
- `VITE_API_URL` правильно указан в `client/.env`

### Ошибка: "Module not found"

```bash
# Переустанови зависимости
cd server && rm -rf node_modules && npm install
cd ../client && rm -rf node_modules && npm install
```

---

## Дополнительно

### Доступ к API

API доступно по адресу: `http://localhost:3000/api`

Примеры:
- `GET /api/finance/stats` — финансовые метрики
- `GET /api/finance/transactions` — транзакции
- `GET /api/proposals` — КП
- `POST /api/auth/login` — вход

### Тестирование API через curl

```bash
# Получить JWT-токен
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@nocto.ru","password":"password"}'

# Получить статистику (замени YOUR_TOKEN)
curl http://localhost:3000/api/finance/stats \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## Следующие шаги

После успешного запуска можешь:

1. Добавить тестовые транзакции через Prisma Studio
2. Создать несколько КП и экспортировать в PDF
3. Протестировать фильтры по датам
4. Посмотреть код и доработать под свои нужды

---

👍 **Готово!** Если есть вопросы — обращайся!

**Nocto Agency** • Ekaterinburg • [nocto.ru](https://nocto.ru)
